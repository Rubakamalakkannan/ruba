#include<stdio.h>
int main()
{
int num;
printf("enter the values");
scanf("%d",&num);
if(num>0)
{
printf("the num is positive");
}
if(num<0)
{
printf("the num is negative");
}
if(num==0)
{
printf("the num is zero");
}
return 0;
}
